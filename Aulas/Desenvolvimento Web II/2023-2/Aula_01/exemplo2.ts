namespace exemplo2 {
    //VAR, LET, CONST
    let numero: number, numero2: number;
    numero = 5;

    let resultado: number;

    resultado = numero + numero;
    console.log("A soma entre " + numero + " com " + numero + "é igual a: " + resultado );
    
    resultado = numero - 4;
    console.log(`A subtração entre ${numero} com 4 é igual a: ${resultado}`);
}